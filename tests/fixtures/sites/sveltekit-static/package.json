{
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build:storybook": "svelte-kit-storybook build",
    "preview:build": "echo vite buildable",
    "build": "vite build"
  },
  "dependencies": {
    "@sveltejs/adapter-static": "^3.0.0",
    "@sveltejs/kit": "^2.0.0"
  }
}
