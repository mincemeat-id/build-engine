# Optional upstream-bot config only. Local developers should install the tracked
# git hooks with `make hooks-install`; `.githooks/` is the canonical local gate.
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.0
    hooks:
      - id: ruff-check
      - id: ruff-format
